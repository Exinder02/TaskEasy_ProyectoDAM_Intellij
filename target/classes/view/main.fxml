<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<BorderPane xmlns="http://javafx.com/javafx/21"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.aitorbenito.taskeasy.MainController"> <top>
    <VBox>
        <Label text="Gestor de tareas - TaskEasy"
               style="-fx-font-size: 18px; -fx-font-weight: bold; -fx-padding: 10;" />
        <MenuBar>
            <Menu text="Archivo">
                <MenuItem text="Nueva tarea" onAction="#agregarTarea"/>
                <MenuItem text="Eliminar" onAction="#eliminarTarea"/>
                <MenuItem text="Modificar" onAction="#modificarTarea"/>
                <SeparatorMenuItem/>
                <MenuItem text="Cerrar sesión" onAction="#cerrarSesion"/>
                <MenuItem text="Salir" onAction="#salirAplicacion"/>
            </Menu>
            <Menu text="Ajustes">
                <MenuItem text="Cambiar tema (Oscuro/Claro)" onAction="#cambiarTema"/>
            </Menu>
            <Menu text="Ayuda">
                <MenuItem text="Acerca de TaskEasy" onAction="#mostrarAcercaDe"/>
            </Menu>
        </MenuBar>
    </VBox>
</top>

    <center>
        <StackPane>
            <TableView fx:id="tablaTareas" VBox.vgrow="ALWAYS">
                <columns>
                    <TableColumn fx:id="colTitulo" text="Título" prefWidth="200"/>
                    <TableColumn fx:id="colDescripcion" text="Descripción" prefWidth="300"/>
                    <TableColumn fx:id="colFecha" text="Fecha limite" prefWidth="120"/>
                    <TableColumn fx:id="colEstado" text="Estado" prefWidth="120"/>
                </columns>
                <columnResizePolicy>
                    <TableView fx:constant="CONSTRAINED_RESIZE_POLICY"/>
                </columnResizePolicy>
            </TableView>


        </StackPane>
    </center>

    <bottom>
        <VBox spacing="10">
            <padding>
                <Insets top="10" right="10" bottom="10" left="10"/>
            </padding>

            <HBox spacing="10" alignment="CENTER_LEFT">
                <Button text="➕ Nueva Tarea" onAction="#agregarTarea"/>
                <Button text="✏️ Modificar Tarea" onAction="#modificarTarea"/>
                <Button text="🗑️ Eliminar Tarea(s)" onAction="#eliminarTarea"/>
            </HBox>

            <HBox fx:id="contenedorLeyenda"
                  spacing="15"
                  alignment="CENTER_RIGHT">
                <padding>
                    <Insets top="5" right="10" bottom="0" left="10"/>
                </padding>
            </HBox>

        </VBox>
    </bottom>

</BorderPane>
